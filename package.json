{
  "name": "zenui",
  "version": "1.0.0",
  "description": "A cross-platform, utility-first, themeable design UI library for React Native and Web",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.12",
    "turbo": "^1.10.12",
    "typescript": "^5.2.2"
  },
  "packageManager": "npm@10.2.0",
  "engines": {
    "node": ">=18"
  }
}